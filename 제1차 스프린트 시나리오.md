# 제1차 스프린트 실습 시나리오

이 문서는 음성 인식 애플리케이션 개발 프로젝트의 제1차 스프린트 실습 시나리오를 안내합니다. 이 스프린트에서는 기본 개발 환경 설정, 저장소 초기화, 최소 기능 구현 및 CI/CD 파이프라인 구성을 목표로 합니다.

## 1. 프로젝트 관리자(PM) 작업

### 1.1. GitHub 저장소 생성 및 초기화 [선행 업무]

1. **GitHub 웹사이트에서 저장소 생성**:
   - [GitHub](https://github.com/) 로그인
   - 오른쪽 상단의 '+' 아이콘 클릭 → 'New repository' 선택
   - 저장소 이름: `speech-recognition-app`
   - 설명: `Flask와 React를 이용한 음성 인식 애플리케이션`
   - 공개 설정: `Public`
   - `Add a README file` 체크
   - `Create repository` 클릭

2. **브랜치 생성**:
   - 메인 페이지에서 'main' 브랜치 드롭다운 클릭
   - 'View all branches' 클릭
   - 'New branch' 클릭
   - 이름: `develop`
   - 'Create branch' 클릭

### 1.2. 프로젝트 보드 생성 [선행 업무]

1. **프로젝트 보드 설정**:
   - 저장소 페이지에서 'Projects' 탭 클릭
   - 'Create a project' 클릭
   - 'Board' 템플릿 선택
   - 프로젝트 이름: `스프린트 1: 기본 환경 구성`
   - 'Create project' 클릭

2. **열(Column) 구성**:
   - `To do`: 할 일
   - `In progress`: 진행 중
   - `In review`: 검토 중
   - `Done`: 완료

### 1.3. 이슈 생성 및 할당 [선행 업무]

1. **이슈 생성** (다음 이슈들을 생성하세요):

   **이슈 1: 프로젝트 구조 초기화**
   ```
   제목: 프로젝트 구조 초기화
   내용:
   # 개요
   프로젝트의 기본 디렉토리 구조를 생성하고 필요한 파일들을 초기화합니다.

   # 작업 내용
   - server/ 디렉토리 생성 (Flask 백엔드)
   - frontend/ 디렉토리 생성 (React 프론트엔드)
   - .github/workflows/ 디렉토리 생성 (CI/CD 설정)
   - .gitignore 파일 생성

   # 담당자
   @dev1

   # 완료 조건
   - 기본 디렉토리 구조 생성 완료
   - develop 브랜치에 병합
   ```

   **이슈 2: Hello World 백엔드 구현**
   ```
   제목: Hello World 백엔드 구현
   내용:
   # 개요
   기본적인 Flask 서버를 구현하여 "Hello World" 메시지를 반환합니다.

   # 작업 내용
   - Flask 앱 초기화
   - 루트 경로('/') 엔드포인트 구현
   - 서버 실행 코드 작성

   # 담당자
   @dev2

   # 완료 조건
   - 서버가 정상적으로 실행됨
   - 브라우저에서 접속 시 "Hello World" 메시지 표시
   - develop 브랜치에 병합
   ```

   **이슈 3: Hello World 프론트엔드 구현**
   ```
   제목: Hello World 프론트엔드 구현
   내용:
   # 개요
   기본적인 React 앱을 구현하여 "Hello World" 메시지를 표시합니다.

   # 작업 내용
   - create-react-app으로 프로젝트 초기화
   - App.js 파일 수정
   - 기본 스타일 적용

   # 담당자
   @dev3

   # 완료 조건
   - React 앱이 정상적으로 실행됨
   - 화면에 "Hello World" 메시지 표시
   - develop 브랜치에 병합
   ```

   **이슈 4: CI/CD 파이프라인 구성**
   ```
   제목: CI/CD 파이프라인 구성
   내용:
   # 개요
   GitHub Actions를 사용하여 CI/CD 파이프라인을 구성합니다.

   # 작업 내용
   - GitHub Actions 워크플로우 파일 작성
   - 빌드 및 테스트 자동화 설정
   - ngrok을 이용한 임시 배포 환경 구성

   # 담당자
   @dev4

   # 완료 조건
   - GitHub Actions 워크플로우 구성 완료
   - PR 생성 시 자동 빌드 및 테스트 실행
   - develop 브랜치에 병합
   ```

2. **프로젝트 보드에 이슈 추가**:
   - 각 이슈 페이지 오른쪽 사이드바의 'Projects' 섹션에서 생성한 프로젝트 선택
   - 모든 이슈를 `To do` 열에 배치

3. **개발자에게 이슈 알림**:
   - 각 이슈 댓글에 담당 개발자 멘션 추가 (예: @dev1, @dev2, @dev3, @dev4)

## 2. 개발자 작업 (모든 개발자)

### 2.1. 로컬 개발 환경 설정 [동시 진행 업무]

1. **PuTTY로 컨테이너 접속**:
   ```
   # 각자의 계정으로 접속 (dev1, dev2, dev3, dev4)
   호스트: localhost
   포트: 2221
   사용자명: dev1 (또는 dev2, dev3, dev4)
   비밀번호: dev1 (또는 dev2, dev3, dev4)
   ```

2. **Git 사용자 구성**:
   ```bash
   git config --global user.name "개발자 이름"
   git config --global user.email "github 이메일"
   git config --global credential.helper store
   ```

3. **저장소 클론**:
   ```bash
   git clone https://github.com/사용자명/speech-recognition-app.git
   cd speech-recognition-app
   ```
   * 클론 시 GitHub 사용자명과 개인 액세스 토큰 입력

4. **develop 브랜치로 전환**:
   ```bash
   git checkout develop
   ```

### 2.2. VSCode 원격 개발 환경 설정 [동시 진행 업무]

1. **Windows PC의 VSCode에서 Remote-SSH 연결**:
   - F1 키 또는 Ctrl+Shift+P 누르고 'Remote-SSH: Connect to Host' 입력
   - 리스트에서 자신의 컨테이너 선택 (예: dev1-container)
   - 비밀번호 입력하여 접속

2. **워크스페이스 열기**:
   - 'Open Folder' 클릭
   - `/home/dev1/speech-recognition-app` 경로 선택 (각자 계정에 맞게 변경)

## 3. 개발자 1 (PM) 작업

### 3.1. 프로젝트 구조 초기화 [이슈 1 해결]

1. **기능 브랜치 생성**:
   ```bash
   git checkout -b feature/project-structure
   ```

2. **기본 디렉토리 구조 생성**:
   ```bash
   # 백엔드 디렉토리
   mkdir -p server
   
   # 프론트엔드 디렉토리
   mkdir -p frontend
   
   # GitHub Actions 워크플로우 디렉토리
   mkdir -p .github/workflows
   ```

3. **.gitignore 파일 생성**:
   ```bash
   cat > .gitignore << 'EOL'
   # Python
   __pycache__/
   *.py[cod]
   *$py.class
   venv/
   env/
   .env
   
   # Node.js
   node_modules/
   npm-debug.log
   yarn-error.log
   yarn-debug.log
   
   # React build
   frontend/build/
   
   # IDE
   .idea/
   .vscode/
   *.swp
   *.swo
   
   # OS
   .DS_Store
   Thumbs.db
   EOL
   ```

4. **변경사항 커밋 및 푸시**:
   ```bash
   git add .
   git commit -m "초기 프로젝트 구조 생성"
   git push -u origin feature/project-structure
   ```

5. **Pull Request 생성**:
   - GitHub 웹사이트에서 저장소로 이동
   - 'Pull requests' 탭 클릭
   - 'New pull request' 클릭
   - base 브랜치: `develop`, compare 브랜치: `feature/project-structure` 선택
   - 'Create pull request' 클릭
   - 제목: "프로젝트 구조 초기화"
   - 내용: "기본 디렉토리 구조 생성 및 .gitignore 파일 추가"
   - 'Create pull request' 클릭

## 4. 개발자 2 작업

### 4.1. Hello World 백엔드 구현 [이슈 2 해결, 이슈 1 종속]

> **주의**: 이 작업은 이슈 1이 완료되고 develop 브랜치에 병합된 후에 시작하세요.

1. **develop 브랜치 최신화 및 기능 브랜치 생성**:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/hello-world-backend
   ```

2. **Flask 앱 기본 코드 작성**:
   ```bash
   cat > server/app.py << 'EOL'
   from flask import Flask, render_template
   from flask_cors import CORS
   
   app = Flask(__name__,
       template_folder='./www',
       static_folder='./www',
       static_url_path='/'
   )
   CORS(app)  # 모든 도메인에서의 접근을 허용
   
   @app.route('/')
   def index():
       return "Hello, World! 이것은 Flask 서버입니다."
   
   if __name__ == "__main__":
       app.run(host="0.0.0.0", port=3000, debug=True)
   EOL
   ```

3. **requirements.txt 생성**:
   ```bash
   cat > server/requirements.txt << 'EOL'
   flask==2.0.1
   flask-cors==3.0.10
   flask-sock==0.5.0
   whisper==1.0.0
   torch==1.9.0
   numpy==1.21.0
   soundfile==0.10.3.post1
   EOL
   ```

4. **변경사항 커밋 및 푸시**:
   ```bash
   git add .
   git commit -m "Hello World 백엔드 구현"
   git push -u origin feature/hello-world-backend
   ```

5. **Pull Request 생성**:
   - GitHub 웹사이트에서 저장소로 이동
   - 'Pull requests' 탭 클릭
   - 'New pull request' 클릭
   - base 브랜치: `develop`, compare 브랜치: `feature/hello-world-backend` 선택
   - 'Create pull request' 클릭
   - 제목: "Hello World 백엔드 구현"
   - 내용: "기본 Flask 서버 구현 및 Hello World 메시지 반환"
   - 'Create pull request' 클릭

6. **서버 테스트** (로컬에서):
   ```bash
   cd server
   python3 app.py
   ```
   
   - 브라우저에서 `http://localhost:3000` 접속하여 "Hello, World!" 메시지 확인

## 5. 개발자 3 작업

### 5.1. Hello World 프론트엔드 구현 [이슈 3 해결, 이슈 1 종속]

> **주의**: 이 작업은 이슈 1이 완료되고 develop 브랜치에 병합된 후에 시작하세요.

1. **develop 브랜치 최신화 및 기능 브랜치 생성**:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/hello-world-frontend
   ```

2. **React 앱 초기화**:
   ```bash
   cd frontend
   npx create-react-app .
   ```

3. **App.js 수정**:
   ```bash
   cat > frontend/src/App.js << 'EOL'
   import React, { useState, useEffect } from 'react';
   import './App.css';
   
   function App() {
     const [message, setMessage] = useState("로딩 중...");
   
     useEffect(() => {
       // 백엔드 서버에서 메시지 가져오기 (추후 구현)
       // 현재는 단순 메시지 표시
       setMessage("Hello, World! 이것은 React 앱입니다.");
     }, []);
   
     return (
       <div className="App">
         <header className="App-header">
           <h1>{message}</h1>
           <p>음성 인식 애플리케이션 - 제1차 스프린트</p>
         </header>
       </div>
     );
   }
   
   export default App;
   EOL
   ```

4. **package.json에 proxy 설정 추가**:
   ```bash
   # package.json 파일을 열고 다음 줄 추가 (JSON 형식에 맞게 추가해야 함):
   # "proxy": "http://localhost:3000",
   
   cat > frontend/package.json << 'EOL'
   {
     "name": "speech-recognition-frontend",
     "version": "0.1.0",
     "private": true,
     "dependencies": {
       "@testing-library/jest-dom": "^5.11.4",
       "@testing-library/react": "^11.1.0",
       "@testing-library/user-event": "^12.1.10",
       "react": "^17.0.2",
       "react-dom": "^17.0.2",
       "react-scripts": "4.0.3",
       "web-vitals": "^1.0.1"
     },
     "scripts": {
       "start": "react-scripts start",
       "build": "react-scripts build",
       "test": "react-scripts test",
       "eject": "react-scripts eject"
     },
     "eslintConfig": {
       "extends": [
         "react-app",
         "react-app/jest"
       ]
     },
     "browserslist": {
       "production": [
         ">0.2%",
         "not dead",
         "not op_mini all"
       ],
       "development": [
         "last 1 chrome version",
         "last 1 firefox version",
         "last 1 safari version"
       ]
     },
     "proxy": "http://localhost:3000"
   }
   EOL
   ```

5. **프론트엔드 빌드**:
   ```bash
   cd frontend
   npm install
   npm run build
   ```

6. **변경사항 커밋 및 푸시**:
   ```bash
   git add .
   git commit -m "Hello World 프론트엔드 구현"
   git push -u origin feature/hello-world-frontend
   ```

7. **Pull Request 생성**:
   - GitHub 웹사이트에서 저장소로 이동
   - 'Pull requests' 탭 클릭
   - 'New pull request' 클릭
   - base 브랜치: `develop`, compare 브랜치: `feature/hello-world-frontend` 선택
   - 'Create pull request' 클릭
   - 제목: "Hello World 프론트엔드 구현"
   - 내용: "기본 React 앱 구현 및 Hello World 메시지 표시"
   - 'Create pull request' 클릭

8. **앱 테스트** (로컬에서):
   ```bash
   cd frontend
   npm start
   ```
   
   - 브라우저에서 `http://localhost:3001` 접속하여 React 앱 확인

## 6. 개발자 4 작업

### 6.1. CI/CD 파이프라인 구성 [이슈 4 해결, 이슈 1 종속]

> **주의**: 이 작업은 이슈 1이 완료되고 develop 브랜치에 병합된 후에 시작하세요.

1. **develop 브랜치 최신화 및 기능 브랜치 생성**:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/ci-cd-pipeline
   ```

2. **GitHub Actions 워크플로우 파일 작성**:
   ```bash
   cat > .github/workflows/ci-cd.yml << 'EOL'
   name: CI/CD Pipeline

   on:
     push:
       branches: [ main, develop ]
     pull_request:
       branches: [ main, develop ]

   jobs:
     build-and-test:
       runs-on: ubuntu-latest

       steps:
       - uses: actions/checkout@v2
       
       - name: Set up Python
         uses: actions/setup-python@v2
         with:
           python-version: '3.10'
       
       - name: Install dependencies
         run: |
           python -m pip install --upgrade pip
           pip install flask flask-cors flask-sock
           pip install pytest
       
       - name: Set up Node.js
         uses: actions/setup-node@v2
         with:
           node-version: '16'
       
       - name: Install frontend dependencies
         run: |
           cd frontend
           npm install
       
       - name: Build frontend
         run: |
           cd frontend
           npm run build
           mkdir -p ../server/www
           cp -r build/* ../server/www/
       
       - name: Test server
         run: |
           cd server
           python -m pytest || true
       
       - name: Start server and expose with ngrok
         if: github.event_name == 'pull_request'
         run: |
           pip install pyngrok
           python -c "from pyngrok import ngrok; url = ngrok.connect(3000); print(f'Server available at: {url}')" &
           cd server
           python app.py &
           sleep 15
           echo "서버가 시작되었습니다. 약 15분 동안 유지됩니다."
           echo "ngrok URL을 확인하세요."
   EOL
   ```

3. **간단한 서버 테스트 파일 생성**:
   ```bash
   mkdir -p server/tests
   
   cat > server/tests/test_app.py << 'EOL'
   import pytest
   from app import app as flask_app

   @pytest.fixture
   def app():
       return flask_app

   @pytest.fixture
   def client(app):
       return app.test_client()

   def test_index(client):
       response = client.get('/')
       assert response.status_code == 200
       assert b"Hello, World" in response.data
   EOL
   ```

4. **변경사항 커밋 및 푸시**:
   ```bash
   git add .
   git commit -m "CI/CD 파이프라인 구성"
   git push -u origin feature/ci-cd-pipeline
   ```

5. **Pull Request 생성**:
   - GitHub 웹사이트에서 저장소로 이동
   - 'Pull requests' 탭 클릭
   - 'New pull request' 클릭
   - base 브랜치: `develop`, compare 브랜치: `feature/ci-cd-pipeline` 선택
   - 'Create pull request' 클릭
   - 제목: "CI/CD 파이프라인 구성"
   - 내용: "GitHub Actions 워크플로우 구성 및 임시 배포 환경 설정"
   - 'Create pull request' 클릭

## 7. 프로젝트 관리자(PM) 작업

### 7.1. PR 검토 및 병합 [선행 업무 완료 후 진행]

1. **각 PR 검토**:
   - GitHub 웹사이트에서 'Pull requests' 탭으로 이동
   - 각 PR을 열어 코드 변경사항 검토
   - 필요한 경우 코멘트 남기기

2. **PR 병합**:
   - 코드 검토가 완료되면 'Merge pull request' 클릭
   - 'Confirm merge' 클릭
   - 이슈 상태 업데이트 (Done으로 이동)

### 7.2. develop에서 main으로 병합 [모든 이슈 완료 후 진행]

1. **PR 생성**:
   - GitHub 웹사이트에서 'Pull requests' 탭 클릭
   - 'New pull request' 클릭
   - base 브랜치: `main`, compare 브랜치: `develop` 선택
   - 'Create pull request' 클릭
   - 제목: "스프린트 1 완료: 기본 환경 구성"
   - 내용: "제1차 스프린트에서 구현한 기본 환경 구성 및 Hello World 애플리케이션 병합"
   - 'Create pull request' 클릭

2. **PR 병합**:
   - 'Merge pull request' 클릭
   - 'Confirm merge' 클릭

### 7.3. 배포 확인 [최종 단계]

1. **GitHub Actions 동작 확인**:
   - GitHub 저장소의 'Actions' 탭에서 워크플로우 실행 상태 확인
   - 성공적으로 완료되었는지 확인

2. **배포된 애플리케이션 확인**:
   - 워크플로우 로그에서 ngrok URL 확인
   - URL로 접속하여 애플리케이션 동작 확인

### 7.4. 스프린트 회고 [최종 단계]

1. **이슈 보드 검토**:
   - 모든 이슈가 'Done' 상태인지 확인
   - 완료되지 않은 이슈 식별 및 다음 스프린트로 이동

2. **회고 미팅 진행**:
   - 팀원들과 함께 스프린트 진행 상황 검토
   - 잘된 점과 개선이 필요한 점 논의
   - 다음 스프린트 계획 수립

## 8. 모든 개발자 작업

### 8.1. 로컬 저장소 동기화 [최종 단계]

```bash
git checkout main
git pull origin main
git checkout develop
git pull origin develop
```

## 주요 URL 및 접속 정보

- **GitHub 저장소**: https://github.com/사용자명/speech-recognition-app
- **로컬 백엔드 서버**: http://localhost:3000
- **로컬 프론트엔드 개발 서버**: http://localhost:3001
